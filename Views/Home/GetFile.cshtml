@model AzStorageAccountPrivareEndP.Models.MyFileClass

@{
    ViewData["Title"] = "GetFile";
}

<h1>GetFile</h1>

<h4>MyFileClass</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="SaveFile" formmethod="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="file" value="Create" name="file" class="btn btn-primary" />
                <div class="form-group">
                <input type="submit" value="Upload" class="btn btn-primary" />
                </div><div class="form-group">
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
